# Endpoint configurable — peut être Google Apps Script, un webhook, Supabase, etc.
# Permet de changer de backend sans toucher au code React.
VITE_LEADS_ENDPOINT=https://script.google.com/macros/s/YOUR_DEPLOYMENT_ID/exec
